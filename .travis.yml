language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t abhinav/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true abhinav/docker-react npm run test